# .env.example  — copy to .env for local development.
# Do NOT commit .env to git (it is already in .gitignore).

# Leave blank in local dev so Vite's proxy handles /api/* automatically.
# Set to https://sheguard-api.onrender.com only when testing against the
# live Render backend from your local machine.
VITE_API_BASE_URL=
